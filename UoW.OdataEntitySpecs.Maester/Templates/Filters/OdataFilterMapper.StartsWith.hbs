namespace {{ProjectNamespace}}.Application.{{EntityName}}s.Specifications
{
    using {{ProjectNamespace}}.Domain.Entities;
    using System;
    using System.Collections.Generic;
    using System.Linq.Expressions;
    using UoW.OData.Knight;

    public partial class {{ClassEntityName}}OdataFilterMapper
    {
        public override Dictionary<string, Func<string, Expression<Func<{{EntityName}}, bool>>>> StartsWithChecks { get; } =
            new Dictionary<string, Func<string, Expression<Func<{{EntityName}}, bool>>>>
            {
                {{#each Properties}}
                { "{{PropertyName}}", (value) => x => x.{{PropertyName}}.StartsWith(value) },
                {{/each}}
            };
    }
}